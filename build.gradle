apply plugin: 'java'
apply plugin: "jacoco"

version = '1.0.0'

description = """Code for refactoring demo"""

sourceCompatibility = 1.8
targetCompatibility = 1.8

tasks.withType(JavaCompile) {
	options.encoding = 'UTF-8'
}

repositories {
     mavenCentral()
}
